# 自动获取模型上下文窗口 - 设计方案

# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询**# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 |# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** |# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** |# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略
# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 →# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└────────────────────────────────────────# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ─────────────────────────# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow:# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
  description?: string;
}> = {
  // OpenAI
  'gpt-4o# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
  description?: string;
}> = {
  // OpenAI
  'gpt-4o': { contextWindow: 128000, provider: 'openai', description: 'GPT-# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
  description?: string;
}> = {
  // OpenAI
  'gpt-4o': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o' },
  'gpt-# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
  description?: string;
}> = {
  // OpenAI
  'gpt-4o': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o' },
  'gpt-4o-mini': { contextWindow: 128# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
  description?: string;
}> = {
  // OpenAI
  'gpt-4o': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o' },
  'gpt-4o-mini': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o Mini' },
# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
  description?: string;
}> = {
  // OpenAI
  'gpt-4o': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o' },
  'gpt-4o-mini': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o Mini' },
  'gpt-4-turbo': { context# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
  description?: string;
}> = {
  // OpenAI
  'gpt-4o': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o' },
  'gpt-4o-mini': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o Mini' },
  'gpt-4-turbo': { contextWindow: 128000, provider: 'open# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
  description?: string;
}> = {
  // OpenAI
  'gpt-4o': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o' },
  'gpt-4o-mini': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o Mini' },
  'gpt-4-turbo': { contextWindow: 128000, provider: 'openai', description: 'GPT-4 Turbo'# 自动获取模型上下文窗口 - 设计方案

> **目标**：新增模型时无需手动修改代码，自动获取上下文窗口并计算压缩阈值

---

## 一、方案对比

| 方案 | 原理 | 优点 | 缺点 | 可行性 |
|------|------|------|------|--------|
| **A. API查询** | 调用模型提供商API获取参数 | 最准确 | 大多数API不返回上下文窗口；增加延迟 | ❌ 不可行 |
| **B. 远程配置** | 从远程JSON/GitHub拉取配置 | 可动态更新；无需发版 | 需要网络；首次启动可能失败 | ✅ 可行 |
| **C. 智能推断** | 根据模型名称规律推断 | 零依赖；即时生效 | 新模型命名不规则时会失效 | ✅ 可行 |
| **D. 混合策略（推荐）** | 本地+远程+推断三层兜底 | 可靠性最高 | 实现稍复杂 | ✅✅ 最佳 |

---

## 二、推荐方案：三层混合策略

```
获取模型上下文窗口的流程：

用户选择模型
    ↓
┌─ 第一层：本地硬编码配置 ──────────────────────┐
│  检查 model-name 是否在本地 MODEL_CONFIG 中     │
│  ├─ 命中 → 返回上下文窗口                      │
│  └─ 未命中 → 进入第二层                        │
└──────────────────────────────────────────────┘
    ↓
┌─ 第二层：远程配置（缓存） ─────────────────────┐
│  检查是否有缓存的远程配置                        │
│  ├─ 有缓存且未过期 → 查找模型 → 命中则返回      │
│  └─ 无缓存或未命中 → 进入第三层                 │
└──────────────────────────────────────────────┘
    ↓
┌─ 第三层：智能推断 ───────────────────────────┐
│  根据模型名称特征推断上下文窗口                   │
│  ├─ 能推断 → 返回推断值 + 标记"不确定"          │
│  └─ 无法推断 → 返回默认值（128k）+ 警告日志     │
└──────────────────────────────────────────────┘
    ↓
后台异步：尝试从远程获取最新配置并更新缓存
```

---

## 三、详细设计

### 3.1 第一层：本地硬编码配置

```typescript
// src/main/services/model-config.ts

/**
 * 本地硬编码的模型配置
 * 优先从这里查找，确保常用模型的准确性
 */
export const LOCAL_MODEL_CONFIG: Record<string, {
  contextWindow: number;
  provider: string;
  description?: string;
}> = {
  // OpenAI
  'gpt-4o': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o' },
  'gpt-4o-mini': { contextWindow: 128000, provider: 'openai', description: 'GPT-4o Mini' },
  'gpt-4-turbo': { contextWindow: 128000, provider: 'openai', description: 'GPT-4 Turbo' },
  'gpt-4': { contextWindow: 8192, provider: '